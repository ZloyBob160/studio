{
  "entities": {
    "User": {
      "title": "User",
      "description": "Represents a user of the application.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The unique identifier for the user, matching Firebase Auth UID."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user.",
          "format": "email"
        },
        "displayName": {
          "type": "string",
          "description": "The display name of the user."
        }
      },
      "required": ["uid", "email"]
    },
    "Conversation": {
      "title": "Conversation",
      "description": "A single chat conversation with the AI analyst.",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "A short title for the conversation."
        },
        "messages": {
          "type": "array",
          "items": {
            "$ref": "#/entities/Message"
          },
          "description": "The messages within the conversation."
        },
        "startTime": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the conversation was created."
        },
        "isFinalized": {
          "type": "boolean",
          "description": "Whether the conversation has been finalized and documents generated."
        },
        "documents": {
          "type": "object",
          "description": "The generated documents after finalization."
        }
      },
      "required": ["title", "messages", "startTime", "isFinalized"]
    },
    "Message": {
      "title": "Message",
      "description": "A single message within a conversation.",
      "type": "object",
      "properties": {
        "role": {
          "type": "string",
          "enum": ["user", "assistant"]
        },
        "content": {
          "type": "string"
        }
      },
      "required": ["role", "content"]
    }
  },
  "auth": {
    "providers": ["anonymous", "password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "Stores user profile information."
    },
    "/users/{userId}/conversations/{conversationId}": {
      "schema": { "$ref": "#/entities/Conversation" },
      "description": "Stores individual conversations for each user."
    }
  }
}
